deploy:
  - helm upgrade --install sentry . --set filestore.filesystem.persistence.size=1Gi --set clickhouse.enabled=false --set kafka.enabled=false --set externalKafka={} --set rabbitmq.enabled=false --set metrics.enabled=false

